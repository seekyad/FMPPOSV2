{
  "name": "bridge",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "FMP store print bridge — runs on the always-on store PC; connects outbound to the POS server and drives the Rongta receipt printer and Niimbot label printer. Built out in Phase 1-2.",
  "scripts": {
    "dev": "node src/index.js"
  }
}
